# إعداد PWA (Progressive Web App)

## ✅ ما تم إنجازه

1. ✅ إنشاء `manifest.json` مع جميع الإعدادات المطلوبة
2. ✅ إنشاء `service worker` (`sw.js`) للتخزين المؤقت والوضع offline
3. ✅ إنشاء مكون `InstallButton` لإضافة زر التثبيت
4. ✅ إضافة `PWAProvider` لتسجيل service worker
5. ✅ تحديث `next.config.ts` مع تحسينات الأداء
6. ✅ تحديث `layout.tsx` مع PWA meta tags

## 📱 إنشاء الأيقونات

لإنشاء أيقونات PWA من `favicon.png`:

```bash
# تثبيت sharp (إذا لم يكن مثبتاً)
npm install --save-dev sharp

# إنشاء الأيقونات
npm run generate-icons
```

سيتم إنشاء الأيقونات في `public/icons/` بالأحجام التالية:
- 72x72
- 96x96
- 128x128
- 144x144
- 152x152
- 192x192
- 384x384
- 512x512

## 🚀 الميزات

### 1. التثبيت كتطبيق
- زر تثبيت يظهر تلقائياً في المتصفحات المدعومة
- دعم iOS و Android و Desktop
- تعليمات واضحة للمستخدمين

### 2. الوضع Offline
- Service Worker يخزن الملفات الأساسية
- استراتيجية Network First مع fallback للـ cache
- يعمل حتى بدون اتصال بالإنترنت

### 3. تحسينات الأداء
- تخزين مؤقت ذكي للملفات
- تحميل أسرع للصفحات
- تقليل استهلاك البيانات

## 📝 ملاحظات

- Service Worker يعمل فقط في `production` mode
- الأيقونات يجب أن تكون موجودة في `public/icons/`
- Manifest.json يجب أن يكون في `public/`

## 🔧 التخصيص

يمكنك تخصيص:
- الألوان في `manifest.json` (theme_color, background_color)
- الأيقونات في `public/icons/`
- Service Worker strategy في `public/sw.js`

